<%= form_with(model: broker_acl, local: true) do |form| %>
  <% if broker_acl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broker_acl.errors.count, "error") %> prohibited this broker_acl from being saved:</h2>

      <ul>
      <% broker_acl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, { class: 'form-control', readonly: true } %>
  </div>

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_field :topic, { class: 'form-control' } %>
  </div>

  <div class="field">
    <%= form.label :read, "Read?" %>
    <%= form.check_box :read %>
  </div>

  <div class="field">
    <%= form.label :read, "Write?" %>
    <%= form.check_box :write %>
  </div>
  <div class="field">
    <%= form.label :read, "Subscribe?" %>
    <%= form.check_box :subscribe %>
  </div>

  <div class="field form-group">
    <%= form.label 'Request' %>
    <%= form.collection_select :provision_request_id, ProvisionRequest.all, :id, :friendly_name, {}, { class: 'form-control', disabled: true } %>
  </div>

  <div class="actions">
    <%= form.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
