<%= form_with(model: public_network) do |form| %>
  <% if public_network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_network.errors.count, "error") %> prohibited this public_network from being saved:</h2>

      <ul>
        <% public_network.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :network_id %>
    <%= form.text_field :network_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
